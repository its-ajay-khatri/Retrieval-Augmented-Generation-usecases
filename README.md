initially install the requirements, run command "pip install -r requirements.txt"

Delete the storage folder as it will be recreated using build_index file(kept storage folder for reference)

then run command "py build_index.py"

then run command "py app.py"
